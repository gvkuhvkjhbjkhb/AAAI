# Reproduce the S2/P3 serving stack. Install the CUDA-specific PyTorch build
# using the same lab image/channel that produced torch 2.11.0+cu128.
vllm==0.25.1
transformers==5.14.1
openai>=1.0
numpy>=1.26,<3
